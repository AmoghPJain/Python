"""
if __name__ == "__main__" is a common pattern used in Python scripts to determine whether the script is being run directly or being imported as a module into another script.
the __name__ variable is a built-in variable that is automatically set to the name of the current module
"""
from TimeProgram import timeofday # TimeProgram is a python file in the current project and timeofday is a function inside it

timeofday

# print(__name__)
